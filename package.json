{
  "name": "violet",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cd run-unicorn-run && live-server",
    "start:console": "cd console && nodemon --quiet --no-stdin"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "trailingComma": "all",
    "overrides": [
      {
        "files": "*.css",
        "options": {
          "printWidth": 40
        }
      }
    ]
  },
  "dependencies": {
    "axel": "^0.0.3",
    "keypress": "^0.2.1",
    "nodemon": "^1.18.9"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "prettier": "^1.16.0"
  }
}
